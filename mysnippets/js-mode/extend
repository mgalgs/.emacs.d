# This was cloned from a TextMate bundle for yasnippet.
# --
/**
 * @class ${1:NameSpace}.${2:ClassName}
 * @extends ${3:BaseClass}
 * ${4:Description}
 */
$1.$2 = Ext.extend($3, {

    initComponent: function() {
        Ext.apply(this, {
            $0
        });

        $1.$2.superclass.initComponent.apply(this, arguments);
    }
});

Ext.reg('${5:xtype}', $1.$2);