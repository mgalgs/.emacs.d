# This was cloned from a TextMate bundle for yasnippet.
# --
constructor: function(config) {
  config = config || {};

  Ext.applyIf(config, {
    $0
  });

  ${2:Ext.Panel}.prototype.constructor.call(this, config);
}${1:,}